# Branch
This branch `source` is used to store the source files for the blog. And the `main` branch is used to store the generated static files. 

# Installation
```bash
npm install -g hexo-cli # Install Hexo globally
npm install # Install project dependencies
```

# Usage
See [Hexo Wiki](https://tesla-sht.github.io/posts/12c56117.html) for more details.